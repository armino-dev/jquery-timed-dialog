{
    "verbose": true,
    "github": {
        "release": true,
        "releaseName": "v${version}",
        "preRelease": false
    },
    "src": {
        "commitMessage": "Release v${version}",
        "tagName": "v${version}",
        "tagAnnotation": "JQuery Timed Dialog Release v${version}",
        "afterReleaseCommand": "echo Successfully released JQuery Timed Dialog v${version}."
    },
    "npm": {
        "publish": true
    }
}
